apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
spec:
  rules:
    - host: "sensorenregister.amsterdam.nl"
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: app
                port:
                  number: 8000
  tls:
    - hosts:
        - sensorenregister.amsterdam.nl
      secretName: tls
