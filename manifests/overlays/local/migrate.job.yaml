apiVersion: batch/v1
kind: Job
metadata:
  name: migrate
spec:
  template:
    spec:
      containers:
        - name: main
          volumeMounts:
            - mountPath: /secrets
              $patch: delete
      volumes:
        - name: secrets
          $patch: delete
