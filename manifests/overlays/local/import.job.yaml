apiVersion: batch/v1
kind: Job
metadata:
  name: import
spec:
  template:
    spec:
      containers:
        - name: main
          command:
            - /app/deploy/wait-for-it.sh
            - database:5432
            - --timeout=30
            - --
            - python
            - manage.py
            - import_api
